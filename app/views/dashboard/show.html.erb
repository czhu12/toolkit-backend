<div class="container mx-auto p-4">
  <div class="my-8">
    <h1>Your scripts</h1>

    <% current_user.scripts.each do |script| %>
      <%= render "scripts/card", script: script %>
    <% end %>
  </div>
</div>
